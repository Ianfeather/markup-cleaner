var markupCleaner, async, events;

async = require('async');

events = require('events');

markupCleaner = (function(_super) {

  __extends(markupCleaner, _super);

  function markupCleaner() {
    
  }

  return markupCleaner;

})(events.EventEmitter);

module.exports = markupCleaner;
